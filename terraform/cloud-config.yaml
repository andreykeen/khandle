#cloud-config
hostname: ${hostname}
package_update: true
package_upgrade: true
packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - gnupg
  - jq
  - mc
  - net-tools
  - software-properties-common

# runcmd:
#   - sed -i '/#net.ipv4.ip_forward/s/^#//g' /etc/sysctl.conf
#   - sysctl -p
